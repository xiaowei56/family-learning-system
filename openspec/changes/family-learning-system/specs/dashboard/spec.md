## ADDED Requirements

### Requirement: Dashboard overview
The system SHALL display a homepage dashboard showing key information at a glance.

#### Scenario: View dashboard
- **WHEN** user logs in and opens the dashboard
- **THEN** the system displays:
  - Today's review count (grouped by subject)
  - Recent wrong problems (last 7 days)
  - Score rate summary (latest exam per subject)
